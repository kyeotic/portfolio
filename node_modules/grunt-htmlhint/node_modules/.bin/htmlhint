#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../htmlhint/bin/htmlhint" "$@"
  ret=$?
else 
  node  "$basedir/../htmlhint/bin/htmlhint" "$@"
  ret=$?
fi
exit $ret
