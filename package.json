{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "history": "^4.7.2",
    "qs": "^6.5.1",
    "react": "^16.2.0",
    "react-document-title": "^2.0.3",
    "react-dom": "^16.2.0",
    "react-motion": "^0.5.2",
    "react-router-dom": "^4.2.2",
    "react-scripts": "1.0.17",
    "react-stonecutter": "^0.3.9"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "sync": "aws s3 sync build s3://tyrsius.com && aws s3 cp build/index.html s3://tyrsius.com/index.html --cache-control max-age=0",
    "deploy-staging": "GENERATE_SOURCEMAP=false npm run build && cd build && now"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true
  }
}
